<% @title = _("Contact us") %>

<%= foi_error_messages_for :contact %>

<h1><%= @title %></h1>

<div id="contact_preamble" class="contact_preamble">

    <ol>
        <li>¿Tienes dudas sobre Publitopia? Te contamos que tenemos una sección de ayuda sobre las preguntas que te puedan surgir sobre esta página.</li>
        <li>Si tienes otra duda que no está escrita aquí, puedes escribirnos a info@munisabiertas.org o bien, llenar el formulario que aquí se te presenta.</li>
        <li>Si quieres contactarnos porque has encontrado información personal. Por favor lee nuestra área de <a href="/help/privacy">privacidad</a>  y si necesitas algo más escríbenos al correo info@munisabiertas.org o también puedes llenar el formulario que aquí se te presenta.</li>
    </ol>

</div>
<%= form_for :contact do |f| %>

  <% if not @user %>
    <p>
      <label class="form_label" for="contact_name">Nombre:</label>
      <%= f.text_field :name, :size => 20 %>
      (o <%= link_to "Ingresa aquí", signin_path(:r => request.fullpath) %>)
    </p>

    <p>
      <label class="form_label" for="contact_email">Correo:</label>
      <%= f.text_field :email, :size => 20 %>
    </p>
  <% end %>

  <p>
    <label class="form_label" for="contact_subject">Asunto:</label>
    <%= f.text_field :subject, :size => 50 %>
  </p>

  <p>
    <label class="form_label" for="contact_message">Mensaje para Publitopia:</label>
    <%= f.text_area :message, :rows => 10, :cols => 60 %>
  </p>

  <p style="display:none;">
    <%= f.label :comment, 'Do not fill in this field' %>
    <%= f.text_field :comment %>
  </p>

  <p class="form_note">
    El equipo de Publitopia está muy feliz de que te comuniques con nosotros, estamos para ayudarte en lo que podamos.
  </p>


  <div class="form_button">
    <%= hidden_field_tag(:submitted_contact_form, 1) %>
    <%= submit_tag "Mandar mensaje", :data => { :disable_with => "Enviando..." } %>
  </div>

<% end %>
